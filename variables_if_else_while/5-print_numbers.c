#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry Point
 *
 * Return: 0 Always (Success)
 */

int main(void)
{
	int n;

	for (n = 48 ; n <= 57 ; n++)
	{
		putchar(n);
	}
	putchar(10);

	return (0);

}

